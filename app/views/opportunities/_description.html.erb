<div class='opportunity_section'>
  <div class='page_subheader'>
    <h4><i class='fa fa-file-text'></i> <%= t('description') %></h4>
  </div>

  <% if @opportunity.description.present? %>
    <%= format_textarea_input(@opportunity.description) %>
  <% else %>
    <p class='none'><%= t('no_description') %></p>
  <% end %>
</div>
