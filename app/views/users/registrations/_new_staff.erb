<h2>City staff sign up</h2>

<p>To post opportunities, you must have an email account that ends with <strong><%= Rails.configuration.x.staff_domains.map { |x| "@#{x}" }.to_sentence(last_word_connector: ', or ') %></strong>.</p>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <h3>1. Create a login</h3>
  <%= f.input :name %>
  <%= f.input :email, required: true %>
  <%= f.input :password, required: true %>

  <h3>2. Confirm your email address</h3>
  After you submit this form, we'll send you an email address with a link. You must click that link to access the system.</p>
  <%= f.button :button %>
<% end %>

<%= render "users/shared/links" %>
